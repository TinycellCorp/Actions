{
    "channel": "${{ env.CHANNEL_ID }}",
    "text": "${{ env.TITLE }}",
    "attachments": [
        {
            "color": "${{ env.COLOR }}",
            "fields": [
                {
                    "title": "Status",
                    "short": true,
                    "value": "${{ env.MESSAGE }}"
                }
            ]
        }
    ]
}