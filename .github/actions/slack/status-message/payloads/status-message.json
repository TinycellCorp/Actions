{
    "channel": "${{ inputs.channel-id }}",
    "text": "${{ inputs.title }}",
    "attachments": [
        {
            "color": "${{ inputs.color }}",
            "fields": [
                {
                    "title": "Status",
                    "short": true,
                    "value": "${{ inputs.message }}"
                }
            ]
        }
    ]
}